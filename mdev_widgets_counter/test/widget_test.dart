import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Demo app smoke test', (WidgetTester tester) async {
    // Basic test - the full app requires provider setup
    expect(true, isTrue);
  });
}
