name: mdev_widgets
description: Dynamic widget configuration with stack-based ID extraction and remote configuration.
version: 0.1.0

environment:
  sdk: ^3.10.8
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  web: ^1.0.0
  web_socket_channel: ^3.0.1
  shared_preferences: ^2.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
